# Backend Main facts

This profile describes the main backend repository. The repository URL is supplied by `BACKEND_MAIN_REPOSITORY`; this document contains project facts only and no setup behavior.

- Runtime: Node.js service.
- Service, persistence, and source layout must be confirmed from the checked-out repository.
- Never put credentials in profile context or logs.
