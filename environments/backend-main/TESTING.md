# Backend testing facts

Use the repository's lockfile and scripts as the source of truth for tests. Run focused tests before the configured validation commands, capture exit codes, and treat independent validation as authoritative.
