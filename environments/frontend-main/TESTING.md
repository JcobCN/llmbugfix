# Frontend testing facts

Use the repository's lockfile and scripts as the source of truth for tests. Prefer the smallest relevant test first, then the configured validation commands. Record command exit codes and output; an agent's claim is not validation evidence.
